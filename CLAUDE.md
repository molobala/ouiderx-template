@AGENTS.md

## Claude Code

Claude Code reads this file as project memory. Keep edits consistent with `AGENTS.md`.

When editing `**/*.omc`, `app.json`, `.native-views.json`, or `.host-functions.json`, also follow `.claude/rules/ouiderx.md`.

